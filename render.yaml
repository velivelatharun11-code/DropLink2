services:
  - type: web
    name: droplink2
    runtime: node
    plan: free
    region: oregon
    branch: main
    buildCommand: npm install --include=dev && npm run build
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
